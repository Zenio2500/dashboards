<% @dashboard = Dashboard.find_by(id: params["dashboard_id"]) %>

<div class="row">
  <div class="col-xs-12">
    <h1>Adicionar 'Nova Tarefa Concluída' ao Dashboard: <%= @dashboard.name %></h1>
  </div>

  <div class="col-md-6 col-md-offset-3 col-xs-12">
  
  <%= form_with model: [current_account, @dashboard, @finished_task] do |form| %>

    <%= render "form", f:form %>

    <%= form.submit "Criar", class: "btn btn-primary" %>
  
  <% end %>  
  </div>
</div>

<p></p>

<div id="ld">
    <%= link_to "Voltar ao Dashboard", account_dashboard_path(current_account, @dashboard), class: 'btn btn-primary btn-sm' %>
</div>